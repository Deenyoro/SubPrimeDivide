# Database Configuration
DATABASE_URL=postgresql://factor:factor_dev_password@db:5432/factordb
POSTGRES_USER=factor
POSTGRES_PASSWORD=factor_dev_password
POSTGRES_DB=factordb

# Redis Configuration
REDIS_URL=redis://queue:6379/0
CELERY_BROKER_URL=redis://queue:6379/0
CELERY_RESULT_BACKEND=redis://queue:6379/1

# API Configuration
API_HOST=0.0.0.0
API_PORT=8080
# CORS_ORIGINS: Comma-separated list of allowed origins for CORS
# For local development: http://localhost:3000,http://frontend:3000
# For production/remote access: Add your server IP, e.g., http://10.2.6.140:3000
CORS_ORIGINS=http://localhost:3000,http://frontend:3000

# Frontend Configuration
# NEXT_PUBLIC_API_URL: The API endpoint URL accessible from the browser
# For local development: http://localhost:8080
# For production/remote access: Use your server IP, e.g., http://10.2.6.140:8080
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=ws://localhost:8080
